{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "stream",
  "tasks": {
    "build": {
      "dependsOn": ["typecheck"],
      "inputs": ["src/**", "css/**", "index.html", "vite.config.ts", "tsconfig.json"],
      "outputs": ["dist/**"]
    },
    "typecheck": {
      "inputs": ["src/**", "test/**", "e2e/**", "tsconfig.json"],
      "outputs": []
    },
    "lint": {
      "inputs": ["src/**", "test/**", "e2e/**", "eslint.config.js", ".prettierrc.json"],
      "outputs": []
    },
    "lint:css": {
      "inputs": ["css/**", ".stylelintrc.json"],
      "outputs": []
    },
    "test": {
      "inputs": ["src/**", "test/**", "jest.config.js", "tsconfig.json"],
      "outputs": ["coverage/**"]
    },
    "test:e2e": {
      "dependsOn": ["build"],
      "inputs": ["src/**", "css/**", "e2e/**", "index.html", "playwright.config.ts"],
      "outputs": ["playwright-report/**", "test-results/**"]
    },
    "build:docs": {
      "dependsOn": ["typecheck"],
      "inputs": ["src/**", "typedoc.json", "tsconfig.json"],
      "outputs": ["docs/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
